import css from '../../build/css';

const constants = {
  color: '#ffffff',
  fontSize: '28px',
};

const header = css`
  color: ${constants.color};
  font-size: ${constants.fontSize};
`;

const title = css`
  color: ${constants.color};
  font-size: 24px;
`;
