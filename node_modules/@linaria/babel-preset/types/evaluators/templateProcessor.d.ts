/**
 * This file handles transforming template literals to class names or styled components and generates CSS content.
 * It uses CSS code from template literals and evaluated values of lazy dependencies stored in ValueCache.
 */
import type { State, StrictOptions, TemplateExpression, ValueCache } from '../types';
import { Core } from '../babel';
export default function getTemplateProcessor({ types: t }: Core, options: StrictOptions): ({ styled, path }: TemplateExpression, state: State, valueCache: ValueCache) => void;
