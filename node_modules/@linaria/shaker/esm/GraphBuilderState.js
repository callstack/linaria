function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import ScopeManager from './scope';
import DepsGraph from './DepsGraph';
export default class GraphBuilderState {
  constructor() {
    _defineProperty(this, "scope", new ScopeManager());

    _defineProperty(this, "graph", new DepsGraph(this.scope));

    _defineProperty(this, "meta", new Map());

    _defineProperty(this, "callbacks", []);

    _defineProperty(this, "context", []);

    _defineProperty(this, "fnStack", []);
  }

  onVisit(callback) {
    this.callbacks.push(callback);
    return () => {
      this.callbacks = this.callbacks.filter(c => c !== callback);
    };
  }

}
//# sourceMappingURL=GraphBuilderState.js.map